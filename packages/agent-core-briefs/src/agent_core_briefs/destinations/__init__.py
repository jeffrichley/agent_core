"""Built-in destinations for the brief framework.

Destinations are loaded by name (``type_id``) via the catalog; a dotted
import path or filesystem-discovery wires them in. Re-exports land in
T16 once the Pepper YAML wiring is in place.
"""
