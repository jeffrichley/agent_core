"""agent_core entry-point hook surface for the brief framework.

Stub — extended in T16 with pluggy hookimpl wiring (fetcher/destination
discovery + alias registration). Declared here so the
``[project.entry-points."agent_core"]`` line in pyproject.toml resolves
during install without erroring.
"""

from __future__ import annotations
