IdentityInjector now shows a cross-session **continuity-not-ready** placeholder when `handoff-status.json` is `pending` for a prior session (Cutover #02), instead of loading possibly stale `handoff.md`. Failed handoff status copy points operators at MEMORY.md and daily summaries. SessionEndWriter treats successful daemon enqueue as pending (not failed). Tests: HandoffWriter delegation, DLQ purge CLI cwd, handoff job endpoint patches, enqueue integration no longer leaks class-level `_extract_handoff`.
