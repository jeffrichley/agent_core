`HandoffJobsEndpoint` collapses bursts of `SessionEnd`-driven `HandoffReady` envelopes into one per routing target per `handoff_publish_dedupe_seconds` window (default 60s). Fixes the `/compact` storm reported in #136.
