`reply()` now strips Discord inbound-only `metadata.discord.*` keys (`guild_id`, `author_id`, `author_display_name`, `is_dm`, `is_bot`) from the inbound side of the shallow merge before publishing the outbound; replies to Discord-routed inbounds now reach the adapter instead of silently dropping at the strict-mode shape validator (#161).
