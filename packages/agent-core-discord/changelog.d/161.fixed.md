Regression test pins the NACK path for `TextMessage` outbounds carrying Discord inbound-only enrichment fields (`guild_id`, `author_id`, `author_display_name`, `is_dm`, `is_bot`); the adapter publishes a yellow `Acknowledgment` instead of silently dropping (#161).
