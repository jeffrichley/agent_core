DM reactions to bot-sent messages now reach the bus. The reaction listener is wired against ``on_raw_reaction_add`` so it fires regardless of message-cache state — matching the existing pattern for poll votes and message edits/deletes (#171).
