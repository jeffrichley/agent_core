`discord.reaction_add` Events now respect the same channel allowlist and `dmPolicy` as inbound messages. Reactions in non-allowlisted channels are silently dropped, matching `on_message` semantics (#180).
